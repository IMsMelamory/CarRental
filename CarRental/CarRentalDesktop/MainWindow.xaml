<Window x:Class="CarRentalDesktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewModel="clr-namespace:CarRentalDesktop.ViewModel"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance viewModel:CarsViewModel}"
        Title="CarRentalClient">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <ListBox Grid.Column="0" 
                 ItemsSource="{Binding Path =Cars}"
                 SelectedItem="{Binding SelectedCar, Mode = TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Padding="0,0,2,0" Text="{Binding Path = Number }" />
                        <TextBlock Padding="0,0,2,0" Text="{Binding Path = Model  }" />
                        <TextBlock Padding="0,0,2,0" Text="{Binding Path = Color }" />
                        <TextBlock Padding="0,0,2,0" Text="{Binding Path = DateRelease}" />
                        <TextBlock Padding="0,0,2,0" Text="{Binding Path = DayPrice }" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Grid Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" Grid.Column="0" ColumnSpan="2" >
                <StackPanel Orientation="Vertical" 
                            Margin="5">
                <TextBlock Text="Номер" />
                <TextBox  Text="{Binding Path= Number, UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Модель" />
                <TextBox Text="{Binding Model,  UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Цвет" />
                <TextBox Text="{Binding Color,  UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Год выпуска" />
                <TextBox Text="{Binding DateRelease,  UpdateSourceTrigger=PropertyChanged}" />
                <TextBlock Text="Цена аренды за день" />
                <TextBox Text="{Binding DayPrice,  UpdateSourceTrigger=PropertyChanged}" />
                </StackPanel>
            </Grid>
            <Button Grid.Row="1"
                    Grid.Column="0"  
                    Height="30"
                    Command="{Binding Path=AddNew}"
                    Content="{Binding Path=ButtonContent}"  />
            <Button Grid.Row="1" 
                    Grid.Column="1" 
                    Height="30"
                    Command="{Binding Path=Remove}" 
                    CommandParameter="{Binding SelectedCar}" 
                    Content="Remove"/>
            <Button Grid.Row="2" 
                    Grid.Column="0"  
                    Grid.ColumnSpan="2"
                    Height="30"
                    Command="{Binding Clear}" 
                    CommandParameter="{Binding SelectedCar}" 
                    Content="Clear" />
        </Grid>
    </Grid>
</Window>